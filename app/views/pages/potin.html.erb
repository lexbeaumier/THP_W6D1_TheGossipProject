<div class="container" style="height:80vh">
    <% @gossips.all.each do |gossip| %>
      <% if gossip.id == @id.to_i%>
        <h3> Titre : <br><%= gossip.title %></h3>
        <p> Contenu :  <br><em><%= gossip.content %></em> </p>
        <p> Prénom de l'auteur :  <br><em><%= gossip.user.first_name %></em></p>
        <p> Ville de l'auteur :  <br><em><%= gossip.user.city.name %></em></p>



        <a href="<%= root_path %>" class="btn btn-primary m-3 rounded-pill">👈 Revenir au menu</a>
        <a href="/potin/auteur/:id" class=" btn-auteur btn btn-primary m-3 rounded-pill">Profil de l'auteur</a>
        <hr>
      <% end %>
    <% end %>
</div>